# Configurações do banco de dados para desenvolvimento local
# Use este arquivo quando rodar o backend localmente (go run main.go)
# O Docker Compose na pasta database expõe o PostgreSQL na porta 55432
DB_HOST=localhost
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=risk_register
DB_PORT=55432

# Configurações da aplicação
GIN_MODE=debug
PORT=8080
